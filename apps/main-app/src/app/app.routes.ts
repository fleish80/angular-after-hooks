import { Route } from '@angular/router';
import { PresentationComponent } from './demos/presentation';

export const appRoutes: Route[] = [
  {
    path: '',
    component: PresentationComponent,
    title: 'Angular After Render APIs - Complete Guide'
  }
];
